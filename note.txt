 # Application      : ref Dommain, 
 # Domain           : không ref ai, chỉ chứa Entities + Enum
 # Infrastructure   : ref Application + Domain, EF Core, BCrypt
 # WebApi           : ref Application, EF Core, JwtBearer, Swagger, (chỉ using Infrastructure trong Program)
            
            
            
            ------##### BƯỚC 1 #####------
- Tạo class DbSeeder (tạo user ban đầu) 
    -> layer Infrastructure/Persistence/DbSeeder.cs
    (mặc định có sẵn 1 tài khoản Admin)
     + check role admin có chưa ? ( làm trước )
     + check user có tồn tại không ( làm sau -> user phụ thuộc role )
     + Add (role + user) vào database

- Tạo interface cho password và HashPassword(BCrypt)
    -> layer Application/Interfaces/Services/IPasswordHasher.cs
    -> layer Infrastructure/Security/BCryptPasswordHasher.cs

- Cấu hình Seed (ở app.Environment.IsDevelopment) trong Program.cs (khi đã có IPasswordHasher)


            ------##### BƯỚC 2 #####------
- Tạo IUserRepository để hiển thị các Task
    -> layer Application/Interfaces/Repositories/IUserRepository.cs

- Thực thi IUserRepository
    -> layer Infrastructure/Repositories/UserRepository.class

- Tạo JwtTokenService (Generate Token)
    -> layer Infrastructure/Security/JwtTokenService.cs
- Config Jwt (Program.cs)

- Tạo AuthService (login , gọi JwtTokenService)
    -> layer Application/Services/AuthService.cs

- DI tất cả vào Program.cs


            ------##### BƯỚC 3 #####------
- Controllers gọi AuthService
    -> layer WebApi/Controllers/AuthController.cs
    (Add và Map Controllers vào Program.cs )


            ------##### BƯỚC 4 #####------
- Cấu hình cho lịch khám(Appointment) và các bảng liên quan(Patient/Doctor + User) : lấy danh sách có phân trang